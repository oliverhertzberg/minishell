#include "../../headers/builtins.h"

t_builtins  do_builtins(char **line) //sending pointer to a string so that we can change it in the memory
{
  t_builtins  b;
  
  ft_strip(line);
  
  return (b);
}
